pipeline {
    agent any

    environment {
        PROJECT_DIR = 'registration-app'  // Path to the project directory containing pom.xml
    }

    stages {
        stage('Checkout') {
            steps {
                // Clone the Git repository with the correct branch name
                git branch: 'main', url: 'https://github.com/sp6862/registration-app.git'
            }
        }
        stage('Build') {
            st
